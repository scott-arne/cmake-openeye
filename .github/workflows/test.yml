name: Test FindOpenEye

on:
  push:
    tags: ['v*']
  workflow_dispatch:

env:
  GCS_BUCKET: openeye-sdks
  # Single source of truth for the OpenEye release exercised by every job.
  # Keep it in step with the SDK filenames in the matrix below.
  OPENEYE_VERSION: "2026.1.0"

jobs:
  test-linux:
    permissions:
      contents: read
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        # SDK filenames are spelled out rather than interpolated from
        # OPENEYE_VERSION: the env context is not available in strategy.matrix,
        # so ${{ env.* }} would expand to an empty string and the download would
        # 404. Keep these in step with OPENEYE_VERSION above.
        include:
          - arch: x86_64
            runner: ubuntu-latest
            container: rockylinux:8
            sdk: OpenEye-toolkits-2026.1.0-redhat-RHEL8-g++8.5-x64.tar.gz
            dnf_repo: powertools
          - arch: aarch64
            runner: ubuntu-24.04-arm
            container: rockylinux:9
            sdk: OpenEye-toolkits-2026.1.0-Ubuntu-22.04-g++11.11-aarch64.tar.gz
            dnf_repo: crb

    runs-on: ${{ matrix.runner }}
    container: ${{ matrix.container }}

    steps:
      - name: Install prerequisites
        run: |
          dnf install -y epel-release
          dnf config-manager --set-enabled ${{ matrix.dnf_repo }}
          # --nobest: Rocky 9 AppStream periodically ships newer -devel packages
          # (e.g. python3-devel-3.9.25-3.el9_7.3) before the matching runtime
          # package lands in baseos. Without --nobest dnf refuses to fall back
          # to a compatible older -devel. --allowerasing covers curl-minimal
          # being replaced by curl on some container images.
          dnf install -y --allowerasing --nobest \
            git curl unzip gcc gcc-c++ cmake ninja-build \
            zlib-devel swig python3-devel

      - uses: actions/checkout@v6

      - name: Authenticate to Google Cloud
        id: gcp-auth
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
          token_format: access_token

      - name: Download OpenEye SDK from GCS
        run: |
          mkdir -p /opt/openeye
          curl -fsSL \
            -H "Authorization: Bearer ${{ steps.gcp-auth.outputs.access_token }}" \
            "https://storage.googleapis.com/${{ env.GCS_BUCKET }}/${{ matrix.sdk }}" \
            -o /tmp/sdk.tar.gz
          tar -xzf /tmp/sdk.tar.gz -C /opt/openeye --strip-components=2
          ls -la /opt/openeye/

      - name: Download OpenEye license
        run: |
          curl -fsSL \
            -H "Authorization: Bearer ${{ steps.gcp-auth.outputs.access_token }}" \
            "https://storage.googleapis.com/${{ env.GCS_BUCKET }}/oe_license.txt" \
            -o /opt/openeye/oe_license.txt

      - name: Configure tests
        run: |
          cmake -S tests -B tests/build -G Ninja \
            -DOPENEYE_ROOT=/opt/openeye

      - name: Build tests
        run: cmake --build tests/build

      - name: Run tests
        env:
          OE_LICENSE: /opt/openeye/oe_license.txt
        run: ctest --test-dir tests/build --output-on-failure

  test-macos:
    permissions:
      contents: read
      id-token: write
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v6

      - name: Install CMake + Ninja + SWIG
        run: brew install cmake ninja swig

      - name: Authenticate to Google Cloud
        id: gcp-auth
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
          token_format: access_token

      - name: Download OpenEye SDK from GCS
        run: |
          mkdir -p ${{ runner.temp }}/openeye
          curl -fsSL \
            -H "Authorization: Bearer ${{ steps.gcp-auth.outputs.access_token }}" \
            "https://storage.googleapis.com/${{ env.GCS_BUCKET }}/OpenEye-toolkits-${{ env.OPENEYE_VERSION }}-macOS-14-clang++16-universal.tar.gz" \
            -o /tmp/sdk.tar.gz
          tar -xzf /tmp/sdk.tar.gz -C ${{ runner.temp }}/openeye --strip-components=2
          ls -la ${{ runner.temp }}/openeye/

      - name: Download OpenEye license
        run: |
          curl -fsSL \
            -H "Authorization: Bearer ${{ steps.gcp-auth.outputs.access_token }}" \
            "https://storage.googleapis.com/${{ env.GCS_BUCKET }}/oe_license.txt" \
            -o ${{ runner.temp }}/oe_license.txt

      - name: Configure tests
        run: |
          cmake -S tests -B tests/build -G Ninja \
            -DOPENEYE_ROOT=${{ runner.temp }}/openeye

      - name: Build tests
        run: cmake --build tests/build

      - name: Run tests
        env:
          OE_LICENSE: ${{ runner.temp }}/oe_license.txt
        run: ctest --test-dir tests/build --output-on-failure

  test-windows:
    permissions:
      contents: read
      id-token: write
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - uses: actions/checkout@v6

      - name: Setup MSVC dev environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Install CMake + Ninja + SWIG
        run: choco install ninja cmake swig -y --no-progress
        shell: powershell

      - name: Setup Python 3.10 (abi3 baseline)
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Authenticate to Google Cloud
        id: gcp-auth
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
          token_format: access_token

      - name: Download OpenEye SDK from GCS
        run: |
          mkdir -p "${RUNNER_TEMP}/openeye-extract"
          curl -fsSL \
            -H "Authorization: Bearer ${{ steps.gcp-auth.outputs.access_token }}" \
            "https://storage.googleapis.com/${{ env.GCS_BUCKET }}/OpenEye-toolkits-${{ env.OPENEYE_VERSION }}-win-x64-MD.zip" \
            -o "${RUNNER_TEMP}/sdk.zip"
          unzip -q "${RUNNER_TEMP}/sdk.zip" -d "${RUNNER_TEMP}/openeye-extract"
          sdk_inner=$(find "${RUNNER_TEMP}/openeye-extract" -maxdepth 1 -mindepth 1 -type d | head -n1)
          echo "OPENEYE_ROOT=${sdk_inner}" >> $GITHUB_ENV

      - name: Download OpenEye license
        run: |
          curl -fsSL \
            -H "Authorization: Bearer ${{ steps.gcp-auth.outputs.access_token }}" \
            "https://storage.googleapis.com/${{ env.GCS_BUCKET }}/oe_license.txt" \
            -o "${RUNNER_TEMP}/oe_license.txt"

      - name: Configure tests
        run: |
          cmake -S tests -B tests/build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DOPENEYE_ROOT="${OPENEYE_ROOT}"

      - name: Build tests
        run: cmake --build tests/build

      - name: Run tests
        env:
          OE_LICENSE: ${{ runner.temp }}/oe_license.txt
        run: ctest --test-dir tests/build --output-on-failure
